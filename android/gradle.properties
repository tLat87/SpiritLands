# -------------------------------
# 🚀 Project-wide Gradle settings
# -------------------------------

# Увеличиваем память JVM для Gradle, чтобы избежать OutOfMemoryError и Java heap space
org.gradle.jvmargs=-Xmx6g -XX:MaxMetaspaceSize=2g -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# Включаем параллельную сборку для ускорения
org.gradle.parallel=true

# -------------------------------
# ✅ AndroidX и совместимость
# -------------------------------

# Используем AndroidX
android.useAndroidX=true

# Jetifier больше не нужен для RN 0.71+
android.enableJetifier=false

# -------------------------------
# 📱 React Native настройки
# -------------------------------

# Указываем, какие архитектуры нужно собирать
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# Включаем новую архитектуру (TurboModules + Fabric)
newArchEnabled=true

# Включаем Hermes (лучше производительность, меньше размер APK)
hermesEnabled=true

# Prefab отключаем — не нужен для RN-проектов
android.prefabEnabled=false

# -------------------------------
# 🛠️ Оптимизация Gradle Daemon
# -------------------------------

# Всегда использовать Gradle Daemon для ускорения сборок
org.gradle.daemon=true

# Кеширование для ускорения пересборок
org.gradle.caching=true